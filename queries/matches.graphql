query matches($count: Int = 15, $after: Int) {
    matchCount
    matches(count: $count, after: $after) {
        id
        type
        startedAt
        finishedAt
        winner {
            id
        }
        rounds {
            id
            playerDecks {
                player {
                    id
                    name
                    lang
                }
                deck {
                    id
                    main
                    side
                    recognizedName
                }
            }
        }
    }
}
