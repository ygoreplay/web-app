query matches($count: Int = 15, $after: Int) {
    matchCount
    matches(count: $count, after: $after) {
        id
        type
        startedAt
        finishedAt
        away {
            ...PlayerDeckInformation
        }
        home {
            ...PlayerDeckInformation
        }
        winner {
            id
        }
    }
}
